[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "plapre"
version = "0.2.0"
description = "Danish text-to-speech synthesis"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "torch",
    "torchaudio",
    "transformers",
    "vllm>=0.15,<0.16",
    "safetensors",
    "soundfile",
    "numpy",
    "num2words",
    "huggingface-hub",
    "kanade-tokenizer @ git+https://github.com/frothywater/kanade-tokenizer",
]

[project.optional-dependencies]
serve = ["fastapi>=0.110", "uvicorn[standard]>=0.29", "python-multipart>=0.0.9"]

[project.scripts]
plapre-serve = "plapre.server:main"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["plapre"]
